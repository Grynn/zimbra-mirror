!{explode vhn_vip_ssl}
# IMAP proxy configuration
# 
server
{
    server_name             ${vhn}; 
    listen                  ${vip}:${mail.imap.port};
    protocol                imap;
    proxy                   on;
    sasl_service_name       "imap";
    starttls                ${mail.imap.tls};
    ssl_certificate         ${ssl.crt};
    ssl_certificate_key     ${ssl.key};
}

