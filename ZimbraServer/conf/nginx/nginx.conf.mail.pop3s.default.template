# POP3S proxy default configuration
#
server
{
    listen              ${mail.pop3s.port};
    protocol            pop3;
    proxy               on;
    ssl                 on;
    ssl_certificate     ${ssl.crt.default};
    ssl_certificate_key ${ssl.key.default};
    sasl_service_name   "pop";
}

