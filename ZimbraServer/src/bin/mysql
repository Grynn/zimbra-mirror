#!/bin/bash

source `dirname $0`/lqshutil || exit 1
lqsetvars mysql_directory mysql_socket liquid_mysql_user liquid_mysql_password
		
exec ${mysql_directory}/bin/mysql -S ${mysql_socket} \
    -u ${liquid_mysql_user} --password=${liquid_mysql_password} "$@"
