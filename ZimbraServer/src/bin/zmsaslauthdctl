#!/bin/bash

if [ x`whoami` != xliquid ]; then
    echo Error: must be run as liquid user
	exit 1
fi

source `dirname $0`/zmshutil || exit 1
zmsetvars \
    zimbra_home \
    zimbra_log_directory

rewriteconfig() {
	if [ x$2 = "x" ]; then
		/opt/zimbra/bin/zmmtaconfig sasl > /dev/null 2>&1
	fi
}

#
# Main
#
case "$1" in
    'start')
		mkdir -p ${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/state
		rewriteconfig
		${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/sbin/saslauthd -a liquid 
        ;;

    'kill')
		cpid=`cat ${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/state/saslauthd.pid`
        kill -TERM "$cpid" 2> /dev/null
        ;;

    'stop')
		cpid=`cat ${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/state/saslauthd.pid`
        kill -TERM "$cpid" 2> /dev/null
		killall -TERM ${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/sbin/saslauthd
        ;;
    
    'restart')
        $0 stop
        $0 start
        ;;
	
	'status')
		cpid=`cat ${zimbra_home}/cyrus-sasl-2.1.21.LIQUID/state/saslauthd.pid`
        kill -0 "$cpid" 2> /dev/null
		exit $?
		;;
    
    *)
        echo "Usage: $0 start|stop|kill|restart|status"
        exit 1
        ;;
esac
